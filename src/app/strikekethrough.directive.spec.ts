import { StrikethroughDirective } from './strikethrough.directive';

describe('StrikekethroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikethroughDirective();
    expect(directive).toBeTruthy();
  });
});
